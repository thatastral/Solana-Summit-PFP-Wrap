{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "rm -rf node_modules package-lock.json && npm install --registry=https://registry.npmjs.org/ --no-package-lock"
}
